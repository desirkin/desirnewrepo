diff --git a/test/social-4f-scope.test.js b/test/social-4f-scope.test.js
index ec03020..1246b57 100644
--- a/test/social-4f-scope.test.js
+++ b/test/social-4f-scope.test.js
@@ -263,6 +263,12 @@ test('SCOPE-8 (9/10). protected surfaces are byte-identical to 9c17372; authorit
     assert.deepEqual(added.sort(), [...authorizedAdded].sort(), `${f}: only the SOCIAL-5 authorized lines were added`);
     assert.deepEqual(removed.sort(), [...AUTHORIZED_DELTA[f].removed].sort(), `${f}: only the SOCIAL-5 authorized lines were replaced`);
   }
+});
+
+// Keep current authority boundaries independently executable when an older
+// historical byte-differential reports unrelated intentional source changes.
+// The historical assertions above remain intact and still report those changes.
+test('SCOPE-9. current catalog consumers and Social authority fences remain exact', () => {
   const tracked = execSync("git ls-files '*.js' '*.mjs'", { cwd: REPO, encoding: 'utf8' }).trim().split('\n');
   const read = (f) => readFileSync(path.join(REPO, f), 'utf8');
   const code = (f) => read(f).split('\n').filter((l) => { const t = l.trim(); return !(t.startsWith('//') || t.startsWith('*') || t.startsWith('/*')); }).join('\n');
