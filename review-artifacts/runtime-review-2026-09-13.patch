diff --git a/lib/data-only-budget.js b/lib/data-only-budget.js
index 59a5e73..732f5db 100644
--- a/lib/data-only-budget.js
+++ b/lib/data-only-budget.js
@@ -19,7 +19,6 @@ export const DEFAULT_DATA_ONLY_LANES = Object.freeze({
   SEC_OFFICIAL: Object.freeze({ maxDailyRequests: 1_080, usdPerRequest: 0 }),
   CFTC_OFFICIAL: Object.freeze({ maxDailyRequests: 1_080, usdPerRequest: 0 }),
   OFAC_OFFICIAL: Object.freeze({ maxDailyRequests: 96, usdPerRequest: 0 }),
-  GDELT_NEWS_DISCOVERY: Object.freeze({ maxDailyRequests: 16, usdPerRequest: 0 }),
   NOAA_SWPC: Object.freeze({ maxDailyRequests: 300, usdPerRequest: 0 }),
   CLOUDFLARE_RADAR: Object.freeze({ maxDailyRequests: 30, usdPerRequest: 0 }),
   COINDESK_NEWS: Object.freeze({ maxDailyRequests: 150, usdPerRequest: 0 }),
@@ -29,8 +28,6 @@ export const DEFAULT_DATA_ONLY_LANES = Object.freeze({
     usdPerRequest: 0,
   }),
   DECRYPT_NEWS: Object.freeze({ maxDailyRequests: 150, usdPerRequest: 0 }),
-  X_OFFICIAL: Object.freeze({ maxDailyRequests: 120, usdPerRequest: 0 }),
-  FARCASTER_OFFICIAL: Object.freeze({ maxDailyRequests: 96, usdPerRequest: 0 }),
 });
 
 const HOST_LANES = Object.freeze({
@@ -44,15 +41,12 @@ const HOST_LANES = Object.freeze({
   "sanctionslistservice.ofac.treas.gov": "OFAC_OFFICIAL",
   "wc2h-sls-prod-public-published.s3.us-gov-west-1.amazonaws.com":
     "OFAC_OFFICIAL",
-  "api.gdeltproject.org": "GDELT_NEWS_DISCOVERY",
   "services.swpc.noaa.gov": "NOAA_SWPC",
   "api.cloudflare.com": "CLOUDFLARE_RADAR",
   "www.coindesk.com": "COINDESK_NEWS",
   "www.theblock.co": "THEBLOCK_NEWS",
   "cointelegraph.com": "COINTELEGRAPH_NEWS",
   "decrypt.co": "DECRYPT_NEWS",
-  "api.x.com": "X_OFFICIAL",
-  "api.neynar.com": "FARCASTER_OFFICIAL",
 });
 
 const utcDay = (ts) => new Date(ts).toISOString().slice(0, 10);
diff --git a/test/data-only-budget.test.js b/test/data-only-budget.test.js
index cc83c8f..cf56874 100644
--- a/test/data-only-budget.test.js
+++ b/test/data-only-budget.test.js
@@ -12,9 +12,9 @@ import {
   loadDataOnlyBudgetCheckpoint,
 } from "../lib/data-only-budget.js";
 
-test("GDELT discovery has an exact zero-cost lane under the deployment daily cap", () => {
-  assert.equal(dataOnlyLaneForRequest("https://api.gdeltproject.org/api/v2/doc/doc?query=crypto"), "GDELT_NEWS_DISCOVERY");
-  assert.deepEqual(DEFAULT_DATA_ONLY_LANES.GDELT_NEWS_DISCOVERY, { maxDailyRequests: 16, usdPerRequest: 0 });
+test("separately journaled discovery is not duplicated in the generic governor", () => {
+  assert.equal(dataOnlyLaneForRequest("https://api.gdeltproject.org/api/v2/doc/doc?query=crypto"), null);
+  assert.equal(DEFAULT_DATA_ONLY_LANES.GDELT_NEWS_DISCOVERY, undefined);
   assert.equal(dataOnlyLaneForRequest("https://evil.example/gdelt"), null);
 });
 
@@ -74,24 +74,47 @@ test("zero-spend runtime refuses any paid lane before startup", () => {
   }
 });
 
-test("official social hosts are explicitly capped while unknown hosts still fail closed", () => {
+test("separately journaled social hosts are not duplicated while unknown hosts fail closed", () => {
   assert.equal(
     dataOnlyLaneForRequest("https://api.x.com/2/tweets/search/stream"),
-    "X_OFFICIAL",
+    null,
   );
   assert.equal(
     dataOnlyLaneForRequest("https://api.neynar.com/v2/farcaster/cast/search"),
-    "FARCASTER_OFFICIAL",
+    null,
   );
   assert.equal(
     dataOnlyLaneForRequest("https://example.com/not-approved"),
     null,
   );
   assert.equal(dataOnlyLaneForRequest("http://api.x.com/2/tweets"), null);
-  assert.equal(DEFAULT_DATA_ONLY_LANES.X_OFFICIAL.maxDailyRequests, 120);
-  assert.equal(DEFAULT_DATA_ONLY_LANES.FARCASTER_OFFICIAL.maxDailyRequests, 96);
-  assert.equal(DEFAULT_DATA_ONLY_LANES.X_OFFICIAL.usdPerRequest, 0);
-  assert.equal(DEFAULT_DATA_ONLY_LANES.FARCASTER_OFFICIAL.usdPerRequest, 0);
+  assert.equal(DEFAULT_DATA_ONLY_LANES.X_OFFICIAL, undefined);
+  assert.equal(DEFAULT_DATA_ONLY_LANES.FARCASTER_OFFICIAL, undefined);
+});
+
+test("deployed fourteen-lane checkpoint imports without resetting any counters", () => {
+  const expected = [
+    "KRAKEN_PUBLIC_REST", "KRAKEN_STATUS", "COINBASE_STATUS", "OKX_STATUS",
+    "KRAKEN_OFFICIAL", "SEC_OFFICIAL", "CFTC_OFFICIAL", "OFAC_OFFICIAL",
+    "NOAA_SWPC", "CLOUDFLARE_RADAR", "COINDESK_NEWS", "THEBLOCK_NEWS",
+    "COINTELEGRAPH_NEWS", "DECRYPT_NEWS",
+  ];
+  assert.deepEqual(Object.keys(DEFAULT_DATA_ONLY_LANES), expected);
+  const dir = mkdtempSync(path.join(os.tmpdir(), "serpent-data-budget-legacy-"));
+  try {
+    const state = createEmptyDataOnlyBudgetState({ ts: Date.UTC(2026, 8, 13) });
+    state.lanes.KRAKEN_PUBLIC_REST.reserved = 17;
+    state.lanes.KRAKEN_PUBLIC_REST.settled = 16;
+    state.lanes.KRAKEN_PUBLIC_REST.succeeded = 15;
+    state.lanes.KRAKEN_PUBLIC_REST.failed = 1;
+    mkdirSync(path.join(dir, "data-only"), { recursive: true });
+    writeFileSync(path.join(dir, "data-only", "budget-current.json"), JSON.stringify(state));
+    const restored = loadDataOnlyBudgetCheckpoint(dir, { lanes: DEFAULT_DATA_ONLY_LANES, maxMonthlyUsd: 0 });
+    assert.deepEqual(restored, state);
+    assert.deepEqual(restored.lanes.KRAKEN_PUBLIC_REST, state.lanes.KRAKEN_PUBLIC_REST);
+  } finally {
+    rmSync(dir, { recursive: true, force: true });
+  }
 });
 
 test("durable reservation is awaited before wire and a failed settlement latches further dispatch", async () => {
diff --git a/tools/data-only-runtime.mjs b/tools/data-only-runtime.mjs
index 85a06ae..cc4319e 100644
--- a/tools/data-only-runtime.mjs
+++ b/tools/data-only-runtime.mjs
@@ -129,6 +129,7 @@ let wideEye = null;
 let rumor2 = null;
 let video = null;
 let market = null;
+let publicDiscovery = null;
 let persistence = null;
 let checkpoints = null;
 let stopped = false;
@@ -231,6 +232,7 @@ if (catalog) {
       DISCOVERY_KALSHI_PAGE_LIMIT: '1000',
     };
     const handle = startPublicDiscovery({ env, dataDir: root, catalogSource, log, signals: false, durableCheckpoint: checkpoints.discovery });
+    publicDiscovery = handle;
     if (handle) handles.push(handle);
   } catch (error) { blockers.PUBLIC_DISCOVERY = bounded(error.message); log(`PUBLIC DISCOVERY blocked: ${blockers.PUBLIC_DISCOVERY}`); }
 }
@@ -275,7 +277,7 @@ function writeRuntimeStatus() {
   const budget = governor.status();
   const currentCatalog = wideEye?.catalogSnapshot?.()?.catalog ?? null;
   const rumorStatus = rumor2?.status?.() ?? null;
-  const discoveryStatus = readDiscoveryStatus(root);
+  const discoveryStatus = publicDiscovery ? readDiscoveryStatus(root) : null;
   const officialFeed = (id) => {
     const provider = rumorStatus?.providers?.[id] ?? null;
     return provider ? {
