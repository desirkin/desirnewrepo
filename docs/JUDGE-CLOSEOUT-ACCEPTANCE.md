# JUDGE closeout acceptance checklist (R01–R16 + section D)

Baseline B = `24eba2ced9e233b71a6a476931dd15a0bb61160f` (tree `39763113084f754daf2c738e889af50ed607e615`). Written BEFORE any
production edit of this repair; the `candidate result` / `status` columns are filled in as each stage lands (a row is
never removed, marked optional or relabelled NOT_RUN to ship). LIVE exercise against the venue is separately NOT_RUN by
authorization; every offline assertion below is mandatory.

Evidence logs (outside the tree, scratch dir `scratchpad/repair/`): `probes-on-B.log` (appendix diagnostic on B, 27/27
"defect reproduced" = 26 witnesses), `red-on-B.stage1.log` … `red-on-B.stage6.log` (the new desired-behaviour suites run
against B, stage by stage, with the actual failure reasons: stage 1 R01/R06 + pg, 2 R02/R03/R04/R05, 3 R07/R08/R09/R15,
4 R10/R11, 5 R12/R13/R14, 6 R16 + section D + pg), `green.log` (all twelve suites serially on the candidate: 76 tests,
76 pass), `final-full.log` (the full serial gate on the committed tree). Baseline full gate for B's tree: `scratchpad/judge/final-full-3.log` (1869 pass, exit 0).

Statuses: OPEN | FIXED_TESTED | BASELINE_ALREADY_CORRECT (counter-evidence named) | BLOCKED (reason named).

Test files (new, desired behaviour, named assertions): `test/judge-repair-authority.test.js` (R01),
`test/judge-repair-accounting.test.js` (R06), `test/judge-repair-exit.test.js` (R02/R04), `test/judge-repair-kraken.test.js`
(R03/R05), `test/judge-repair-restart.test.js` (R07), `test/judge-repair-runtime.test.js` (R08/R09/R15),
`test/judge-repair-evidence.test.js` (R10/R11), `test/judge-repair-evaluation.test.js` (R12/R13),
`test/judge-repair-arming.test.js` (R14), `test/judge-repair-projection.test.js` (R16), `test/judge-repair-pg.test.js`
(competing PostgreSQL sessions, restoration failure), `test/judge-repair-section-d.test.js` (D1–D6 over PostgreSQL).
Witness → owner index (ticket): R01 W01 W21 W22 · R02 W02 · R03 W03 W04 · R04 W05 · R05 W06 W07 · R06 W09 W10 W19(valuation)
· R07 W08 W24 W19(pins) · R08 W20 W23 · R09 W25 W26 · R10 static · R11 W18 · R12 W15 W16 · R13 W17 · R14 W11 W12 W13 W14 ·
R15/R16 integration.

| assertion ID | required outcome | production function / caller | test name | baseline result | candidate result | evidence-log location | status |
|---|---|---|---|---|---|---|---|
| R01-01 | KILL latched after the durable intent and before the send: zero `submitEntryWithProtection` calls once deferred SAFETY work resumes; the unsent order is CANCELLED and its reservation released | `execution/dispatcher.js dispatchEntry` → `execution/authority.js entryAuthority` (immediately before the send, after every await) | authority `R01-01` | RED on B: W01 — 1 send after KILL (`probes-on-B.log`) | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-02 | LIVE authorization expired between intent and send: zero sends, order cancelled | dispatcher `dispatchEntry` + `entryAuthority` (authorization expiry / kind) | authority `R01-02` | RED on B (no pre-send check) | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-03 | release / key / policy / code binding changed since the authorization: zero sends | `entryAuthority` binding clauses from the composition context (`composeJudge` supplies `authorityContext`) | authority `R01-03` | RED on B | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-04 | allocation ceiling applied transactionally: a reservation beyond the remaining allocation (ceiling 10 < funded cash 500) is refused by the reducer; reinvestment NONE keeps realized gains out of the deployable amount | `execution/reducer.js RESERVATION_OPENED` → `allocationRemaining` | authority `R01-04` | RED on B: W21 — reservation 100.8 accepted under ceiling 10 | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-05 | the size search sees the smaller of available cash and remaining allocation (smaller legal size, never the whole cash) | `judge/risk.js riskBudgetFor` / `admitCandidate` (`cashAvailable` = min) | authority `R01-05` | RED on B (budget.cash = 500 under ceiling 10) | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-06 | a released / lost writer closes the sender: the memory journal fences a released epoch, the dispatcher marks WRITER_LOST, stops adapter admission, and a later dispatch sends nothing | `execution/journal.js` memory `append` (lease check), `dispatcher.commit` (`writer.held()`), `dispatchEntry` | authority `R01-06` | RED on B: W22 — released writer still commits | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-07 | competing actual PostgreSQL sessions: a writer whose lock session was terminated cannot commit (LOCK_LOST), a successor's epoch fences it (EPOCH_FENCED) | `execution/journal.js` PG `append` (lock-session `held()` + row epoch under FOR UPDATE) | pg `R01-07` | RED on B (append ignores the lock session) | GREEN: test/judge-repair-pg.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log + stage6 (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-08 | an already-sent ambiguous liability (UNCERTAIN send) keeps its reservation and stays DISPATCH_UNCERTAIN even when authority is later refused | reducer `RESERVATION_RELEASED` (RELEASE_WHILE_ORDER_OPEN), dispatcher | authority `R01-08` | GREEN on B (reducer.js RELEASE_WHILE_ORDER_OPEN; test/judge-reducer RED-03) | GREEN on the candidate (test/judge-repair-authority.test.js; green.log, final-full.log) | repair/red-on-B.stage1.log (B) · repair/green.log · repair/final-full.log | BASELINE_ALREADY_CORRECT — authority `R01-08` passes on B as well (red-on-B.stage1.log `ok 12`): reducer `RELEASE_WHILE_ORDER_OPEN` + judge-reducer `RED-03` on B; the candidate keeps the assertion |
| R01-09 | the instrument spec changed (digest / not online) before the send: zero sends | `entryAuthority` spec clause (`specOf`) | authority `R01-09` | RED on B | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-10 | the fee contract digest changed / expired before the send: zero sends | `entryAuthority` fee clause (`feeOf`) | authority `R01-10` | RED on B | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-11 | no fresh usable quote (book stale beyond the policy age or feed impaired) before the send: zero sends | `entryAuthority` quote clause (`feed.health`) | authority `R01-11` | RED on B | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-12 | the reservation no longer covers the intent (released / consumed) before the send: zero sends | `entryAuthority` reservation clause | authority `R01-12` | RED on B | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-13 | NEW-exposure authority is separate from owned protective reductions: under KILL / REDUCE_ONLY the entry is refused while the Watch's protective close of owned inventory proceeds (one `closeResidual`), bounded by owned base | `execution/authority.js reductionAuthority` used by `watch/watch.js advanceExit`; `entryAuthority` in the dispatcher | authority `R01-13` | PARTIAL on B (paper adapter admission only; no shared law) | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R01-14 | the Judge's pre-commit permission and the dispatcher's pre-send check are ONE shared function (static: both import `entryPermission` from `execution/authority.js`) | `judge/judge.js decide`, `execution/dispatcher.js` | authority `R01-14` | RED on B (no shared module) | GREEN: test/judge-repair-authority.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R02-01 | cancel of the protective child acknowledged → confirmed reconciliation through the race BEFORE any sale: a late stop fill of the whole quantity → zero sells | `watch/watch.js advanceExit` phase RECONCILING → `dispatcher.reconcile` → `adapter.reconcile` | exit `R02-01` | RED on B: W02 — sale 0.001 with 0 reconciliations | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R02-02 | partial late stop fill → ONE serialized sale of exactly the confirmed net residual | same | exit `R02-02` | RED on B | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R02-03 | the mock venue reports unrelated base inventory (balance 5 BTC): the sale is never sized from the venue balance, only from owned residual | same + reducer OVERSELL | exit `R02-03` | RED on B (no reconciliation step) | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R03-01 | a fresh adapter hydrates native / client / position ownership from the journal before observing the venue: a fill of an uncertain dispatch (matched by cl_ord_id through ClosedOrders) is booked, never expired | `execution/kraken-adapter.js hydrate` called by `dispatcher.restart` before `reconcile` | kraken `R03-01` | RED on B: W03 — order expired, base 0 | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R03-02 | reconciliation queries open AND terminal orders (ClosedOrders paged to exhaustion) and Ledgers paged to exhaustion | kraken `reconcile` | kraken `R03-02` | RED on B (ClosedOrders never queried; Ledgers one page) | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R03-03 | response shape / count / cursor exhaustion validated: an empty page with a positive count is INCOMPLETE (never COMPLETE); a malformed count or trades object is FAILED | kraken `reconcile` | kraken `R03-03` | RED on B: W04 — COMPLETE after 1 empty page | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R03-04 | execution-id dedup is durable and not capped at 4096: the 1st execution replayed after 4100 later ones is still a DUPLICATE | `execution/reducer.js EXECUTION_RECORDED` | kraken `R03-04` | RED on B (MAX_EXEC_IDS eviction) | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R04-01 | two confirmed native children become two journal PROTECTIVE_STOP orders; protection is the SET (children) with coverage = Σ ACTIVE child qty; aggregate ACTIVE only when coverage equals owned | dispatcher `mapAdapterEvent`, reducer `PROTECTION_STATE` (`protection.children`) | exit `R04-01` | RED on B: W05 — one journal order, second child overwrote the pointer | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R04-02 | a PENDING child is not ACTIVE: coverage counts only confirmed children; under-coverage past the confirm window → PROTECTION_INVALID exit | reducer + `watch/watch.js evaluateReasons` | exit `R04-02` | RED on B | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R04-03 | over-coverage (children exceed owned base) → MISMATCH + PROTECTION_MISMATCH restriction (never an oversell) | reducer `PROTECTION_STATE` | exit `R04-03` | RED on B | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R04-04 | base fees reduce the protected quantity of the child (paper child qty = net base) | `execution/paper-adapter.js createChildStop` | exit `R04-04` | GREEN on B (adapters P02/E06/E07) | GREEN on the candidate (test/judge-repair-exit.test.js; green.log, final-full.log) | repair/red-on-B.stage2.log (B) · repair/green.log · repair/final-full.log | BASELINE_ALREADY_CORRECT — unchanged paper child sizing (`createChildStop` net base) — existing judge-adapters `P02/E06/E07` pass on B (final-full-3.log) and on the candidate; no production change to that law |
| R04-05 | a confirmed child for a FLAT / unknown position is an orphan: EXTERNAL_ACTIVITY + RECONCILIATION_REQUIRED, never attributed | dispatcher `mapAdapterEvent` | exit `R04-05` | RED on B (FLAT position accepted a new synthesized stop) | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R04-06 | amendments name one child; a child below the venue minimum is MISMATCH | reducer `PROTECTION_AMEND`, dispatcher minima | exit `R04-06` | RED on B (minimum not checked) | GREEN: test/judge-repair-exit.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R05-01 | the documented native permission identifiers `query-funds`, `query-open-trades`, `modify-trades` are KNOWN (not ambiguous); anything outside the verified vocabulary stays AMBIGUOUS; no name is invented (remaining capabilities are proven by documented endpoint probes in preflight: CancelOrder unknown-txid, ClosedOrders, GetWebSocketsToken) | `execution/kraken-adapter.js redactKeyInfo`, `preflight` | kraken `R05-01` | RED on B: W06 — the three identifiers → PERMISSION_PROOF_AMBIGUOUS | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R05-02 | the key-info DTO is closed: only allowlisted facts survive; the echoed key / name / unknown fields never do | `redactKeyInfo` | kraken `R05-02` | GREEN on B (adapters E12/E08) | GREEN on the candidate (test/judge-repair-kraken.test.js; green.log, final-full.log) | repair/red-on-B.stage2.log (B) · repair/green.log · repair/final-full.log | BASELINE_ALREADY_CORRECT — closed key-info DTO — existing judge-adapters `E12/E08` pass on B (final-full-3.log); the candidate assertion in kraken `R05-01/R05-02/R05-03` holds (the combined test is RED on B only for R05-01) |
| R05-03 | a withdrawal / deposit / margin capability present → FORBIDDEN_PERMISSION_PRESENT | `redactKeyInfo` | kraken `R05-03` | GREEN on B for display labels; extended to native identifiers | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R05-04 | the authenticated WebSocket host / path is FIXED (`wss://ws-auth.kraken.com/v2`); a caller-supplied url is refused | `connectExecutions` | kraken `R05-04` | RED on B: W07 — foreign host opened | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R05-05 | verified available funds exclude holds (BalanceEx `balance - hold_trade`); arming a ceiling above available (but below total) is refused | `preflight` (BalanceEx), `judge/arming.js buildLiveAuthorization` via `composition.arm` | kraken `R05-05` | RED on B (Balance total used) | GREEN: test/judge-repair-kraken.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage2.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R06-01 | exact basis allocation over NET acquired inventory including base fees: buy 0.001 (fee 0.00001 BTC), sell all 0.00099 for 99 → realized −1, cash 499 | reducer `recomputeRealized` | accounting `R06-01` | RED on B: W09 — realized 0 | GREEN: test/judge-repair-accounting.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R06-02 | FEE_ADJUSTMENT dedup by native identity (`ref`): same ref + same content → DUPLICATE_ADJUSTMENT (no second debit); same ref + different content → ADJUSTMENT_IDENTITY_CONFLICT; INVENTORY_ADJUSTMENT dedup by `cause` | reducer `FEE_ADJUSTMENT` / `INVENTORY_ADJUSTMENT` | accounting `R06-02` | RED on B: W10 — cash debited twice | GREEN: test/judge-repair-accounting.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R06-03 | the production composition publishes durable VALUATION from the Watch's conservative marks: equity = cash + liquidation, marks recorded per position, drawdown / day P&L from THIS account, unknown when a held position has no usable book | `judge/composition.js tick` → `watch.marks()` → VALUATION commit | accounting `R06-03` | RED on B: W19 — valuation stays INITIAL 500 | GREEN: test/judge-repair-accounting.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R06-04 | flow-adjusted session equity / drawdown latches through the production path (deposit does not lift drawdown) | reducer `VALUATION` through composition | accounting `R06-04` | reducer GREEN (RED-06), production path RED (no caller) | GREEN: test/judge-repair-accounting.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R06-05 | gain restrictions are account-specific: the lock level derives from THIS account's session P&L, never the legacy ledger's daily lock | `judge/composition.js lockLevel` → `execution/reducer.js gainLockLevel` | accounting `R06-05` | RED on B (legacy `dailyLockStatus()`) | GREEN: test/judge-repair-accounting.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R06-06 | correlation windows are synchronized: the 60 most recent COMPLETE periods common to both assets are used (identical shifted series join with r = 1); fewer than 60 common periods → UNKNOWN | `judge/risk.js computeClusters` | accounting `R06-06` | RED on B (shifted windows → no join) | GREEN: test/judge-repair-accounting.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R07-01 | a Watch restarted on a position with FINAL R restores its per-unit target basis (durable in POSITION_R) | reducer `POSITION_R` (`targetPerUnit`), `watch/watch.js track` | restart `R07-01` | RED on B: W08 — targetPerUnit null | GREEN: test/judge-repair-restart.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R07-02 | an in-progress exit survives restart: a fresh Watch continues the exit phase, never a second competing sell, first-fill time / trail / highest bid retained | `watch/watch.js track` (exit restore from WATCH_STATE) | restart `R07-02` | RED on B (phase NONE after restart) | GREEN: test/judge-repair-restart.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R07-03 | a fresh PAPER adapter restores its native children (stops), exec-id progression and depletion checkpoint from the journal / checkpoint; reconciliation is COMPLETE only when every open journal order is known | `execution/paper-adapter.js` (`restore`), `judge/composition.js` (checkpoint file) | restart `R07-03` | RED on B: W24 — no stops after restart, COMPLETE claimed | GREEN: test/judge-repair-restart.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R07-04 | paper exec ids continue after the highest journaled id (no identity collision → no false DUPLICATE) | paper adapter `restore` | restart `R07-04` | RED on B | GREEN: test/judge-repair-restart.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R07-05 | held pins survive restart: the composition re-admits every held / pending symbol as HELD before the feed connects | `judge/composition.js start` | restart `R07-05` | RED on B: W19 — pinned 0 | GREEN: test/judge-repair-restart.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R08-01 | clock qualification is wired to real inputs: LIVE start qualifies from `serverTime`, requalifies periodically (WS ping / REST Time), durable CLOCK_ANCHOR; failure → CLOCK_UNTRUSTED blocks entries | `judge/composition.js qualifyClock` (start / tick) | runtime `R08-01` | RED on B: W20 — no `.qualify(` caller | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R08-02 | bucket deadlines are independent of the 250 ms heartbeat: the scheduler runs on its own 25 ms cadence; a job submitted between heartbeats runs (not QUEUE_DELAY_EXPIRED) | `judge/composition.js start` (scheduler timer) | runtime `R08-02` | RED on B: W23 — expired | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R08-03 | deterministic ranking before reservation: two candidates in one bucket with cash for one → the higher reward/risk candidate reserves regardless of receipt order | `judge/judge.js` bucket admission gate → `judge/risk.js rankCandidates` | runtime `R08-03` | RED on B (`rankCandidates` never called) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R08-04 | entry HTTP / case verification cannot starve safety: a hanging entry send does not block adapter fill / protection handling (separate serialized safety and entry pumps in the dispatcher and the adapter); case verification runs off the main thread | `execution/dispatcher.js pump`, `execution/kraken-adapter.js pump`, `judge/case-verify-worker.js` | runtime `R08-04` | RED on B (one pump awaits the entry job) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R08-05 | bounded queues with an explicit overflow consequence: safety overflow latches OVERLOAD (entries refused, fills still never dropped); entry overflow refuses the candidate | dispatcher `enqueue` | runtime `R08-05` | RED on B (log only) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R08-06 | ticks / exits are serialized: overlapping `onTick` calls never start two exits or two sells | `watch/watch.js onTick` (re-entrancy guard), composition `tick` | runtime `R08-06` | RED on B | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R08-07 | producers stop before the drain: after `stop()` no feed message reaches the adapter / Watch / Judge, admission is closed, then the writer is released (order recorded) | `judge/composition.js stop` | runtime `R08-07` | RED on B (feed still ingests after stop) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R09-01 | book snapshots are deeply immutable (levels frozen) | `execution/feed.js snapshotOf` | runtime `R09-01` | RED on B: W25 | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R09-02 | the tape's `depthOf` bound through `bindTape` governs truncation on snapshots and updates (exact subscribed depth) | `execution/feed.js truncate` | runtime `R09-02` | RED on B: W26 | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R09-03 | the documented Kraken v2 checksum example (docs "Book Checksum": CRC32 3310070434) reproduces through the feed's checksum path | `execution/feed.js checksum` | runtime `R09-03` | not covered on B (only synthetic CRCs) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R09-04 | startup loads instrument specs and bar history through the SHARED transport (allowlisted public endpoints) when no spec fixture is injected; cold CLI startup proves it | `judge/composition.js start` / `commands.js runMode` | runtime `R09-04`, section-d `D1` | RED on B (empty specs → candidates refused) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R09-05 | gaps, missing heartbeats and future / laggy timestamps invalidate coverage and health | `execution/feed.js coverage` / `health` / `applyTrade` | runtime `R09-05` | RED on B | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R10-01 | lawful catalyst normalization from verified v2 evidence: `claims[].status PRIMARY_CONFIRMED`, `normalizedSubject` bound to the coin, `claimType` in the closed taxonomy, `claimLinks[] {claimRef, sourceRef, kind PRIMARY_CONFIRMATION}` to a source with `sourceType` official and `authorityClass OFFICIAL`; the packet is accepted by the REAL v2 validator; social / echo / corroborated variants refuse | `judge/intake.js primaryConfirmedCatalyst` | evidence `R10-01` | RED on B (invented fields `asset`, `claimKind`, `sourceType OFFICIAL`) | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R10-02 | `setCatalystEvent` has a production caller: the composition delivers the verified catalyst of a consumed case to the Judge candidate | `judge/composition.js` (case refresh → `judge.setCatalystEvent`) | evidence `R10-02` | RED on B (no caller) | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R10-03 | Watch falsifier delivery: a later verified RETRACTED / CONTRADICTED primary claim for a held asset yields THESIS_FALSIFIED | composition `falsifiers` → `watch/watch.js` | evidence `R10-03` | RED on B (`falsifiers` never supplied) | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R10-04 | cache reuse verifies the referenced bytes: a change to any bundle member (not only the four indexed files) invalidates the cached verdict | `judge/intake.js createCaseVerifier bytesKey` | evidence `R10-04` | RED on B (four files only) | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R10-05 | absorption feature windows match the frozen episode (VWAP / prior windows anchored at the frozen trigger T, not at D) | `judge/setups.js` / `judge/judge.js fastFacts` | evidence `R10-05` | RED on B (`fast.vwap60` recomputed at D) | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R10-06 | MARKET_DIRECT / CASE_ENRICHED / CATALYST_CASE amendment retained (closed decision schema) | `judge/contract.js decisionError` | evidence `R10-06` | GREEN on B (decisions J06/J09) | GREEN on the candidate (test/judge-repair-evidence.test.js; green.log, final-full.log) | repair/red-on-B.stage4.log (B) · repair/green.log · repair/final-full.log | BASELINE_ALREADY_CORRECT — closed decision schema — existing judge-decisions `J06/J09` pass on B (final-full-3.log); the candidate assertion in evidence `R10-05/R10-06` holds (the combined test is RED on B only for R10-05) |
| R11-01 | sealed recordings: a closed manifest with an ordered segment inventory (index, bytes, messages, sha256, first / last receipt clock), state COMPLETE on a clean stop, INTERRUPTED otherwise | `judge/recorder.js` (`manifest.json`) | evidence `R11-01` | RED on B (no manifest) | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R11-02 | a missing middle or tail segment (later segments retained) is an integrity failure on reopen / replay / verify, never a silently shorter recording | `readRecording`, `verifyRecording`, `commands.js verify` | evidence `R11-02` | RED on B: W18 — 1 row silently | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R11-03 | a recorder failure closes admission: FEED_IMPAIRED (source recorder) is latched and entries refuse while exits continue | composition `tick` (recorder state) | evidence `R11-03` | RED on B | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R11-04 | bounded decision / exit snapshots are persisted (atomic, digest-named) BEFORE dispatch / sale | composition `snapshotStore` used by Judge (before the intent commit) and Watch (before `closeResidual`) | evidence `R11-04` | RED on B | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R11-05 | `verify --recording` cannot return complete for partial data (no manifest / INTERRUPTED / digest mismatch → ok:false) | `commands.js verify` | evidence `R11-05` | RED on B | GREEN: test/judge-repair-evidence.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage4.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R12-01 | D1 stresses DISPLAYED depth at 100 / 50 / 25 % for the SAME residual size | `judge/challengers.js d1Feature` | evaluation `R12-01` | RED on B: W15 — q halved | GREEN: test/judge-repair-evaluation.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R12-02 | D3 membership carries the target (`self`) plus ≥ 5 peers and the feature evaluates the target return | `peerMembership`, `d3Feature` | evaluation `R12-02` | RED on B: W16 — TARGET_ENDPOINTS_MISSING | GREEN: test/judge-repair-evaluation.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R12-03 | D2 uses real post-known-at intervals: the response window must be fully inside continuous coverage that started at or before the known-at floor | `d2Feature` (`coverage`) | evaluation `R12-03` | RED on B (no coverage input) | GREEN: test/judge-repair-evaluation.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R13-01 | independent simulated accounts per arm from ONE decision / fill stream: CASH never trades (net 0, closed 0) while REF carries the trades; a challenger arm whose verdict excludes a position reports a different net | `judge/challengers.js evaluateArms` used by `commands.js evaluate` | evaluation `R13-01` | RED on B: W17 — identical | GREEN: test/judge-repair-evaluation.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R13-02 | predeclared splits are durable: the first evaluation declares (declaredTs), later evaluations reuse the declaration; groups first seen later are UNDECLARED, never holdout | `commands.js evaluate` (`splits-<account>.json`) | evaluation `R13-02` | RED on B (recomputed each time, declaredTs null) | GREEN: test/judge-repair-evaluation.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R13-03 | evaluation reads bounded pages of durable history (all DECISION_RECORDED events, beyond the 256-entry projection window) | `commands.js evaluate` (`journal.page`) | evaluation `R13-03` | RED on B (state.decisions only) | GREEN: test/judge-repair-evaluation.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R13-04 | complete outcome reporting: every configured arm reports SCORED or UNSCORABLE with a reason; the actual CLI `evaluate` arm loop is exercised | `commands.js evaluate` via `runCli` | evaluation `R13-04` | RED on B | GREEN: test/judge-repair-evaluation.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R14-01 | ONE shared semantic validator for manifest generation / reopen / approval / arm: rehashing a BLOCKED manifest into CANDIDATE is rejected (recomputed blockers must match; rehashing is integrity, not qualification) | `judge/arming.js manifestSemanticsError` used by `manifestError`, `approveRelease`, `owner.js readManifest`, `composition.arm` | arming `R14-01` | RED on B: W12 — approved | GREEN: test/judge-repair-arming.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R14-02 | null / empty / negative evaluation is not qualification: null net P&L, missing evaluation digest, empty source prefixes → BLOCKED | `prepareRelease` | arming `R14-02` | RED on B: W11 — CANDIDATE | GREEN: test/judge-repair-arming.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R14-03 | a separate bounded CANARY permission can run canary mode: a CANARY authorization enters LIVE_ARMED and only CANARY_ENTRY intents pass (reducer, dispatcher, Watch duration bound) | reducer `MODE_TRANSITION` / `ORDER_INTENT`, dispatcher `entryAuthority`, judge (canary duration) | arming `R14-03` | RED on B: W14 — ARM_REQUIRED | GREEN: test/judge-repair-arming.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R14-04 | qualified canary evidence is required for the ordinary LIVE arm (a COMPLETED, reconciled canary on THIS account under the same release); absent → CANARY_EVIDENCE_REQUIRED | `buildLiveAuthorization` (`canaryEvidence`), reducer `ACCOUNT_AUTHORIZED` LIVE_ARM | arming `R14-04` | RED on B: W13 | GREEN: test/judge-repair-arming.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R14-05 | fail closed on dirty / unknown code identity: a dirty worktree or unknown tree yields no digest; `buildLiveAuthorization` refuses CODE_IDENTITY_UNKNOWN | `judge/owner.js codeTreeDigest`, `buildLiveAuthorization` | arming `R14-05` | RED on B (null digest accepted when approval carried null / dirty tree hashed as clean) | GREEN: test/judge-repair-arming.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R14-06 | durable audit and challenge consumption happen BEFORE authority: an unwritable audit refuses the intent; a challenge is consumed exactly once even when the phrase fails | `owner.js verifyOwnerIntent`, `consumeChallenge` | arming `R14-06` | RED on B (audit best-effort; consume after verify) | GREEN: test/judge-repair-arming.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R14-07 | owner-intent rate limits persist across CLI invocations (durable limiter file): the 6th attempt after 5 failures is RATE_LIMITED even with the correct password in a fresh process context | `owner.js verifyOwnerIntent` (durable limiter) | arming `R14-07` | RED on B (in-memory limiter) | GREEN: test/judge-repair-arming.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage5.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R15-01 | one acquisition / start / drain / release law across CLI / fly / UI: the composition owns the venue slot claim on LIVE `start()` (before any private call) and releases it only after a verified SHUTDOWN reconciliation; an uncertain order keeps the slot held | `judge/composition.js start / stop`, `commands.js liveRun` | runtime `R15-01` | RED on B (claim / release in the CLI only) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R15-02 | WebSocket handles are stored and closed on stop | composition `stop` (`executions.close`) | runtime `R15-02` | RED on B (handle discarded) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R15-03 | reconnect after a socket close with a complete reconciliation (AFTER_GAP → COMPLETE clears RECONCILIATION_REQUIRED) | kraken adapter reconnect + composition | runtime `R15-03` | RED on B (no reconnect) | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R15-04 | periodic fallback reconciliation / health when the executions channel is silent | composition `tick` (LIVE) | runtime `R15-04` | RED on B | GREEN: test/judge-repair-runtime.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage3.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R15-05 | the cold-start path: actual CLI (`run-paper` over PostgreSQL / `run-observe`) with a scripted transport composes, loads specs through the transport, starts, drains and stops (writer released) | `bin/judge.js runCli` → `commands.js runMode` | section-d `D1-cli` | not covered on B | GREEN: test/judge-repair-section-d.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R16-01 | bounded queues / pages / checkpoints: the paper depletion checkpoint is written on stop and restored; pages stay bounded | composition `stop`, paper adapter `checkpoint` | projection `R16-01` | RED on B (checkpoint never written) | GREEN: test/judge-repair-projection.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R16-02 | the UI / projection shows the residual OWNED base (confirmed − sold − base fees), with confirmed / sold facts | composition `projection` | projection `R16-02` | RED on B (`base: confirmedBase`) | GREEN: test/judge-repair-projection.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R16-03 | the projection is bound to account / run mode / revision; a projection for another account or mode is REJECTED by the reader and projects no posture; the cockpit view rejects a cross-account file | `state/execution-projection.js readExecutionProjection({ expected })`, `ui/server.js judgeView`, `state/machine.js` | projection `R16-03` | RED on B (unbound) | GREEN: test/judge-repair-projection.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R16-04 | restoration failure never initializes a fresh USD 500: a corrupt / unverifiable projection refuses to start (RESTORE_FAILED); `initAccount` refuses an existing account | composition `start` (`replayVerify`), `initAccount` | pg `R16-04` | PARTIAL on B (init refuses; start does not verify) | GREEN: test/judge-repair-pg.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage1.log + stage6 (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| R16-05 | E09 closed: a late adapter callback after `stop()` is fenced (no commit, no throw); E10 closed: re-subscription after a gap (R15-03); file failures: an unwritable projection directory is a logged, reported failure (`projectionWriteFailed`) that never stops the run | dispatcher `applyAdapterEvent` after close, composition `publishProjection` | projection `R16-05` | PARTIAL on B (named uncovered) | GREEN: test/judge-repair-projection.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| D1 | PostgreSQL + scripted transport + sealed evidence end to end through the actual CLI: cold `run-paper` (specs through the transport, recording sealed COMPLETE, verify ok) | `bin/judge.js`, composition, recorder | section-d `D1` | not covered on B | GREEN: test/judge-repair-section-d.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| D2 | PAPER interrupted restore over PostgreSQL: a protected position, the process stops mid-position, a fresh adapter / Watch restore the stop, the stop triggers and the position closes FLAT | composition restart path | section-d `D2` | RED on B (W24) | GREEN: test/judge-repair-section-d.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| D3 | KILL / authorization expiry / lock loss at the deferred boundary: zero NEW sends, the owned protective close remains eligible | dispatcher / Watch / authority | section-d `D3` | RED on B (W01) | GREEN: test/judge-repair-section-d.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| D4 | real v2 catalyst / correction through the composition: a verified PRIMARY_CONFIRMED case reaches the Judge candidate; a later RETRACTED claim falsifies the held position | composition + intake + Watch | section-d `D4` | RED on B | GREEN: test/judge-repair-section-d.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| D5 | recorder failure / broken recording prefix / untrusted clock block additions while exits continue | composition + recorder + clock | section-d `D5` | RED on B | GREEN: test/judge-repair-section-d.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |
| D6 | separate evaluation accounts diverge (CASH vs REF vs a restricted arm); release / canary / arm are distinct (canary evidence gates the ordinary arm; a canary authorization runs canary mode) | evaluation + arming + reducer | section-d `D6` | RED on B (W13 / W14 / W17) | GREEN: test/judge-repair-section-d.test.js (green.log 76/76; final-full.log) | repair/red-on-B.stage6.log (RED on B, real reason) · repair/green.log · repair/final-full.log | FIXED_TESTED |

## Semantic corrections to existing tests (old law → new law, negative proof preserved)

Each entry names the test, the old law, the new law and the negative proof that is preserved.

- judge-watch `W01` / `W09` — old law: one queue drained safety-then-entry in a fixed order; new law (R08): two independent pumps, `['entry','safety']` both progress, the safety pump never waits behind entry HTTP. Negative proof kept: an entry cannot starve a cancel (runtime `R08-04`).
- judge-e2e-pg `E2E-2` tail — old law: the test claimed / released the venue owner slot by hand around a run; new law (R15): `start()` claims the slot (own epoch) and `stop()` releases it only after a COMPLETE shutdown reconciliation. Negative proof kept: a stale epoch or an unreconciled release is refused (`EPOCH_FENCED`, `RELEASE_UNRECONCILED`); pg `R15-01` retains the slot on an INCOMPLETE shutdown.
- judge-composition (recorder) — old law: a clean stop reported state `STOPPED`; new law (R11): a sealed recording is `COMPLETE` (manifest with the segment inventory), anything else is `INTERRUPTED` / `RECORDING`. Negative proof kept: verify never says complete for partial data (evidence `R11-02`).
- judge-adapters `E12` / `E08`, judge-arming `liveRoutes`, judge-e2e-pg routes, judge-repair fixtures — fixture completions only: the scripted venue now answers the documented endpoints the adapter really calls (`BalanceEx`, `ClosedOrders` with `closetime=both`, the `CancelOrder` unknown-txid probe, `GetWebSocketsToken`, `Ledgers` counts). No expectation was weakened; a venue that omits one of them fails preflight (`CAPABILITY_<X>_NOT_PROVEN`).
- judge-decisions (warm / ignite), judge-e2e-pg driver — fixture completion: the scripted wire carries a heartbeat every second because a 15 s silence is now a liveness gap (R09). Negative proof kept: a silent socket breaks coverage (runtime `R09-05`).
- judge-evaluation `F03/D2` — old law: the response class was computed from whatever trades / books existed; new law (R12): both windows must lie inside continuous trade coverage that began at or before the pre-window start (`coverage` input, `RESPONSE_WINDOW_NOT_COVERED`). Negative proof kept in the same test: no coverage → no class; coverage that begins inside the pre-window → no class.
- judge-reducer `RED-07`, judge-repair-authority `R01-02..R01-05`, judge-arming `A01`/`A02`/`A03`, judge-e2e-pg `E2E-2`, section-d `D3`/`D5` — old law: any LIVE_ARM authorization with limits + key committed; new law (R14): a COMPLETED, reconciled canary under the SAME release on THIS account must precede it (`CANARY_EVIDENCE_REQUIRED`; fixture `canaryCompleted` in `test/helpers/judge.js`, P&L-neutral). Negative proofs added: `RED-07` and section-d `D6` refuse without evidence and under another release; `A01` refuses a null code digest (`CODE_IDENTITY_UNKNOWN`).
- judge-arming `approvedRelease` fixture, judge-e2e-pg release fixture — fixture completion: a CANDIDATE manifest now needs an `evaluationDigest` and non-empty `sourcePrefixes` (R14-02). Negative proof kept: arming `R14-02` blocks each missing element.
- judge-arming `A02-cli` — the consumed-phrase refusal is now named `CHALLENGE_CONSUMED` (the challenge is consumed durably BEFORE verification, R14-06); the regex accepts the new name alongside the old ones. Negative proof kept: the first attempt burns the phrase, right or wrong (arming `R14-06`).
- judge-arming `A07` — old law: an unbound projection file projected a posture; new law (R16): a projection carries `accountId` / `runMode` / `revision` or every reader rejects it (`UNBOUND`). Negative proofs added: projection `R16-03` (cross-account, cross-mode, regressed revision).
- judge-repair-pg `R01-07` — the test terminates ONLY the advisory-lock session of its own account (before: every advisory-lock holder in the database, which killed a concurrently running suite's writer in a parallel run). Same law, precise target.

## No-substitution checks (ticket)

- KILL test observes zero new-entry transport calls after deferred work resumes — R01-01 / D3.
- oversell test supplies unrelated venue inventory — R02-03.
- restart test creates fresh adapter / Watch instances — R07-02 / R07-03 / D2.
- allocation test arms LESS than funded cash — R01-04.
- catalyst test uses a packet accepted by the real existing verifier — R10-01 / D4.
- replay test deletes an interior segment while retaining later segments — R11-02.
- challenger test observes different decisions / fills — R13-01 / D6.
- approval test rehashes invalid content and shared semantics must still reject — R14-01.
- PostgreSQL writer / claim tests use competing actual DB sessions — R01-07 / R16-04.
- safety tests keep entry HTTP / case work slow while fill / protection handling runs — R08-04.
- cold-start test invokes the actual CLI / composition with scripted transport — R15-05 / D1.

## Focused completion, revision 2 (2026-09-09) — acceptance rows

Reviewed baseline: `fbb89f7d58c643ab177a3fd1421184fdcc98a3f6` (tree `47e8848a70c4cc99f0629f2f2775dfd3b1be28ba`, parent
`1193098a196f910b0839fb7dd6e1699ccb002466`, branch `claude/cobra-phase-c1-setup-n9yy6r`). Node v22.22.2 here (the reviewer ran
v24.19.0). Evidence logs outside the tree (`scratchpad/fc/`): `baseline-guarded.log` + `baseline-guard.jsonl` (the reviewed
tree under the enforced guard with PostgreSQL: 1950 tests, 1950 pass, exit 0, GATE_EXIT=1 with 19 unexpected
`WEBSOCKET ws-auth.kraken.com:443` attempts — judge-arming 5, judge-e2e-pg 3, judge-repair-arming 6, judge-repair-kraken 1,
judge-repair-pg 2, judge-repair-runtime 2 — the known RED N02 witness; the reviewer saw 14 because the two PG suites did not
run there), `fc1.log` (the six repaired suites: 35/35, zero attempts), `fc2b.log`/`fc3c.log`/`fc4c.log` (I / S / H suites),
`final-full.log` (the full serial guarded gate on the committed tree). Statuses: OPEN | FIXED_TESTED | NOT_RUN (named).

Honest note on the earlier R13 rows: R13-01 ("independent simulated accounts per arm from ONE decision / fill stream") is a
filtered replay of one recorded ledger; it is now labelled `HISTORICAL_ATTRIBUTION` and is NOT the strategy comparison. The
strategy comparison is S01 (independent REPLAY accounts consuming the same input stream with their own depletion). R13-02's
durable file declaration (`splits-<account>.json`) stays only behind the legacy `evaluate`; the experiment authority is the
migration-9 record store (H01–H05). D02 reproduces the legacy split defect (A left in development, no purge).

| ID | required outcome | production boundary | test (file · name) | evidence log | status |
|---|---|---|---|---|---|
| N01 | a test-only outbound guard denies fetch / WebSocket / net / tls / dns / dgram to non-loopback hosts before the transport opens, records each attempt outside the repo without secrets, follows spawned Node children with replaced env objects, keeps loopback HTTP / PG normal; self-tests run isolated under an explicit selftest identity | `test/helpers/offline-guard.mjs`, `tools/offline-gate.mjs` | offline-guard · N01-a/N01-b/N01-c | fc1b.log | FIXED_TESTED |
| N02 | the 14/19 external WebSocket attempts are reproduced as the baseline witness (denied, recorded exactly once, the application still gets a handle) and closed: every LIVE composition in the six suites receives a scripted executions-channel lifecycle (subscribe + token, ack, sequences, gap, drop, reconnect + fresh token, owner close); the ordinary suite has ZERO attempts | `test/helpers/scripted-ws.js`, six test files, `execution/kraken-adapter.js` unchanged | offline-guard · N02; judge-repair-runtime · R15-02/R15-03 (scripted lifecycle) | baseline-guard.jsonl (19), fc1.log (0), final-full.log gate | FIXED_TESTED |
| I01 | the real composition records one sealed stream: feed bytes, nominations, instruments, fee, history retrievals, the consumed case with verifier output + digest, control changes, one capture sequence; verifyBundle proves seal + order + typed records; bundle stores rebuild known-at gated sources; cold CLI replay; interior loss / reorder / duplicate / future evidence / digest / version / binding refuse | `judge/recorder.js`, `judge/experiment-bundle.js`, `judge/composition.js` capture hooks, `judge/history.js onRetrieved`, `judge/case-source.js verified` | judge-experiment-bundle · I01 | fc2b.log | FIXED_TESTED |
| I02 | a legacy raw-only COMPLETE recording stays readable / replayable and is LIMITED with named missing capabilities; legacy nominations are the command's inputs, never records | `judge/experiment-bundle.js replayCapabilities`, `judge/recorder.js replayRecording` | judge-experiment-bundle · I02 | fc2b.log | FIXED_TESTED |
| I03 | > 520 nominations flow through the bounded preparation and are all captured; the per-kind recorder bound INTERRUPTS naming the bound; the store bound refuses explicitly; the CASE byte bound refuses | `judge/recorder.js maxTypedRecords`, `judge/experiment-bundle.js BUNDLE_LIMITS` | judge-experiment-bundle · I03 | fc2b.log | FIXED_TESTED |
| S01 | independent USD 500 REPLAY arms over the composition-recorded bundle: REF enters and closes at the planned target (+16.74), CASH never trades, D4 funded exits by NATIVE_STOP at a different net (+14.69), D4 matched pairs the episode, ablation measures but ignores FI15/FI60, seeded thinning exact, D1 UNKNOWN refuses with a verdict record, identical replays identical | `judge/experiment-replay.js`, `judge/judge.js armRule / challengerVerdicts`, `judge/setups.js ablate`, `watch/watch.js exitPolicy`, `judge/composition.js` | judge-experiment-replay · S01 | fc3c.log | FIXED_TESTED |
| S02 | persisted tie order; same-time records in capture order before same-time timers; backwards receipt clocks counted, the clock never runs back; a venue gap opens a new epoch in every arm | `judge/experiment-replay.js` TIE_ORDER_LAW | judge-experiment-replay · S02 | fc3c.log | FIXED_TESTED |
| S03 | delayed case availability: complete-but-not-received is unknown at D (MARKET_DIRECT); received-before enriches (CASE_ENRICHED with refs) and makes the catalyst arm scorable | `judge/experiment-bundle.js createBundleStores.caseSource`, `judge/intake.js consumeCase` (re-run) | judge-experiment-replay · S03 | fc3c.log | FIXED_TESTED |
| S04 | arm definitions closed: only FI15/FI60_POSITIVE of RANGE_IGNITION ablatable; research seams refused outside REPLAY; no D1+D2+D3+D4 combination; matched needs REF_COMBINED | `judge/setups.js ABLATABLE_CLAUSES`, `judge/composition.js`, `judge/judge.js` | judge-experiment-replay · S04; judge-focused-fences · C01 | fc3c.log | FIXED_TESTED |
| S05 | the real CLI `replay-experiment` writes the report; unknown arm / tampered bundle exit 3; `verify --bundle`; the reference policy against a bundle recorded under another fee contract is REFUSED per arm (FEE_BINDING_MISMATCH) | `judge/commands.js`, `bin/judge.js` | judge-experiment-replay · S05 | fc3c.log | FIXED_TESTED |
| S06 | D2 / D3 positive and negative offline paths from declared research inputs (WHALE / PEER records): UNSCORABLE with the capability named; NO_RULE admits; a classed response decides; not-yet-observed and late inputs are UNKNOWN; missing peer endpoints UNKNOWN (never ALLOW); exact-window endpoints decide by residual sign | `judge/challengers.js d2Feature (UNKNOWN)`, `judge/judge.js challengerVerdicts / returnsFor` | judge-experiment-replay · S06; judge-evaluation · F03/D2 (updated law) | fc3c.log | FIXED_TESTED |
| S07 | censoring: an open position at the end is CENSORED with a factual mark (equity = cash + mark) or an unknown mark (equity null) after a disconnect; matched reports the unpaired episode | `judge/experiment-replay.js censoredPosition`, `watch/watch.js marks` | judge-experiment-replay · S07 | fc3c.log | FIXED_TESTED |
| S08 | arm identity bound to experiment / code / policy / rule version / source prefix / seed; binding mismatch REFUSES; a stopped run and a fresh replay of the sealed prefix are the same state (head, revision, depletion) | `judge/experiment-replay.js armBinding / stopAtSeq` | judge-experiment-replay · S08 | fc3c.log | FIXED_TESTED |
| H01 | the declaration persists exact UTC boundaries once from a future duration, binds everything, refuses past starts unless exploratory (no holdout), refuses malformed input; append-only digest chain, tamper detected, revision conflict refused | `judge/experiment.js declareExperiment`, `judge/experiment-store.js` | judge-experiment-holdout · H01 | fc4c.log | FIXED_TESTED |
| H02 | first-known UTC assignment with horizons; whole-group purge across cutoffs / inside embargoes (both sides); PENDING outcomes and publication floors; late arrivals keep the UTC rule; out-of-window named | `judge/experiment.js assignGroup(s)` | judge-experiment-holdout · H02 / D02 | fc4c.log | FIXED_TESTED |
| H03 | split evaluations over a REAL replay report count only their groups' closed net; the holdout is never persisted through the split door; binding mismatch refused; no dev/val look after the opening | `judge/experiment.js evaluateSplit / persistEvaluation / groupsFromReport` | judge-experiment-holdout · H03 | fc4c.log | FIXED_TESTED |
| H04 | lock needs both looks and locks once; opening needs the lock and the completed window; the opening record exists BEFORE any result; concurrent openings serialize to one; resume under the same run id; another run id refused; the evaluation names only the locked arm; second evaluation refused; the chain is what the records say | `judge/experiment.js lockCandidate / openHoldout / evaluateHoldout / holdoutChain` | judge-experiment-holdout · H04 | fc4c.log | FIXED_TESTED |
| H05 | the durable PostgreSQL authority (migration 9) behind the real CLI: declare once, evaluate dev/val, refuse HOLDOUT, lock, open (opening precedes result), second opening refused, chain re-verified, `prepare-release --experiment` carries the store's chain, a tampered embedded chain refused, stale revision refused | `persistence/schema.js` v9, `judge/experiment-store.js`, `judge/commands.js` | judge-experiment-holdout · H05 | fc4c.log | FIXED_TESTED |
| D01 | the release validator consumes the chain: missing → BLOCKED, broken order / binding / digest / hand-made flag → INVALID, a real chain removes the blocker; legacy manifests unqualified; approval never passes a blocked chain; `verify --manifest --experiment` compares with the store | `judge/arming.js releaseBlockers / holdoutChainError / HOLDOUT_CHAIN_SCHEMA` | judge-experiment-holdout · D01; H05 | fc4c.log | FIXED_TESTED |
| D02 | the reproduction case (A..F; A ends 10000; dev .5 / val .1666666667 / embargo 0): legacy leaves A DEVELOPMENT and E/F HOLDOUT with no purge (defect witnessed); the prospective law purges A whole, B C dev, D validation, E F holdout | `judge/challengers.js predeclareSplits` (legacy, witnessed) vs `judge/experiment.js` | judge-experiment-holdout · H02 / D02 | fc4c.log | FIXED_TESTED |
| C01 | protected bytes (config, package, lockfile, doctrine, samples, legacy contracts, migrations 1–8) untouched; no new dependency; research seams refused outside REPLAY; the offline experiment never opens the database | `test/judge-focused-fences.test.js` | judge-focused-fences · C01 | final-full.log | FIXED_TESTED |
| C02 | input-mode semantics preserved; scoring needs no sensor beyond the market inputs (no all-sensors gate); evaluateArms labelled HISTORICAL_ATTRIBUTION; the guard is versioned, the gate exists and is documented; every acceptance id has a row; no test grants itself a self-test identity | `test/judge-focused-fences.test.js` | judge-focused-fences · C02 | final-full.log | FIXED_TESTED |

NOT_RUN (by authorization, named): live preflight, canary, model / provider smoke, paper deployment, activation, funding.
