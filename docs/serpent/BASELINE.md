# Serpent baseline — 2026-09-14

Branch `serpent/baseline` on `desirkin/desirnewrepo`. GitHub tip = this branch (the repo-attached session pushes after every green ticket). orphan history (no common ancestor with `main`; `main` is the 2026-09-12 state, the Replit data-only series lived only in Replit and was recovered from bundles).

## Suite: GREEN — 3,039 tests, 0 fail, 0 cancelled, 0 outbound (serial, offline guard, real PostgreSQL 16) — confirmed at 5afee32 by the working session, 2026-09-14

## Commits since the assembled release candidate (29b0d7a), one ticket each
- `fdf93d3` — durable-store timeout timer was unref'd; a stalled port never timed out (hang).
- `7abf365` — six architecture fences red since the Replit series: X provider routed through the social-time boundary (`utcDayLabel`); live wiring points named once (`test/helpers/composition-roots.js`); R2A-76+77 now catches dynamic imports.
- `cbe2de5` — **reservation never released after a paper fill** (availableCash −368 on a $500 account after one trade; a second trade was impossible). Fixed at the dispatcher; `test/judge-reservation-settlement.test.js`.
- `f6978cf` … `c90f053` — test glob scoped to real tests; JUDGE-PAPER-AUDIT §4.5 written and FROZEN_FOR_PAPER re-pinned; sensor inventory regenerated from its derivation; fence pins moved to the audited laws; the Judge differential keeps its behavioral proof (the 2026-09-12 Judge and today's Judge make identical decisions on identical inputs with the new ports absent).
- `0f752f2` `7dca1f8` `1b7e7a8` — docs in the repo (docs/serpent/): philosophy, map, plan, baseline, the learning/ finding (no superseded drafts; nothing atticked), and the runtime-unification design.
- `5dbe8d9` `af77a84` — runtime unification step 1: the data-only composition body moved verbatim to lib/serpent-runtime.js; entry keeps the safety pins. No behavior change; fences name the spine.
- `679f358` — runtime unification step 2: spine → lib/external-quota.js + lib/collectors.js with injectable starters; test/serpent-runtime-composition.test.js RC-1..3 (order, options, status, reverse stop, lock, fail-closed). Targeted fences green; full run to be confirmed by the worker.
- `65a48eb` — lean trim step 1: one bankroll, one daily-lock law; legacy JSONL ledger retired to `attic/`; `execution/ledger-view.js` + projection `dailyLock`/`ledger`; audit §4.6.
- `5afee32` — worker standing orders + queue (docs/serpent/README.md); baseline at 679f358.
- `cc17c9c` — runtime unification step 3: fly.js is the mode-switched root (`SERPENT_MODE` derived fail-closed; bare start refuses); PAPER runs on the spine — one lock per data dir any mode, runtime-owned persistence signals, external-quota restore, collector additions (market catalogs + broad Kraken + public discovery); DATA_ONLY delegates to the spine. Audit §4.7; frozen digests unchanged. `test/serpent-paper-spine.test.js`, `test/serpent-mode-law.test.js`.
- `0533251` — runtime unification step 4: in-process cockpit. `data:only-ui` is ONE process — the shim pins the full DATA-ONLY posture and enters fly.js; the DATA_ONLY branch serves ui/server.js in-process after the spine (PERSIST-0A §2), judgeRun null; the two-process supervisor is retired; honest `entrypoint` in the status file.
- `b0f5373` — runtime unification step 5: mode-agnostic paths. Canonical `serpent/runtime.lock` + `serpent/runtime-status.json` in both modes; byte-equal `data-only/` mirrors for one release; readers prefer canonical with mirror fallback; a pre-step-5 legacy lock still refuses. PR-4.
- `839bb62` — PERSIST-1 step 1: object-store substrate (adapter contract + FILESYSTEM backend + env-NAME fail-closed config gate). `test/object-store.test.js`.
- `7cab55e` — PERSIST-1 steps 2+3: the self-describing manifest, the changed-only uploader (never prunes durable evidence), restore-on-boot (digest-verified, never clobbers a newer local append), and the spine wiring (restore before any consumer reads, mirror on a 60 s timer, `collectors.objectStore` in the status) for both modes. `test/object-manifest`/`object-uploader`/`object-restore`, spine PR-5.
- `f7f65b8` — docs: David's decided doctrine (learning target, exit law, sizing, cadence, paper realism, venue, Strategy 6 IFR + Strategy 7 SDR) added to PHILOSOPHY.md.
- `179c127` — Ticket 3 step 1: the decision yardstick — pure 5m bite + 15m continuation log-return scorer (labels.js discipline). `test/decision-yardstick.test.js`.
- `e060db5` — Ticket 3 step 2: the dormant decision-outcome recorder + durable idempotent store; matures DECISION_RECORDED via the journal's public page() (no frozen change), sources injected. `test/decision-outcome-recorder.test.js`.
- `7e79eef` — Ticket 3 step 3: the broad-Kraken 1m series source + always-on PAPER wiring in fly.js (the data clock records every PAPER decision, dormant). `test/broad-kraken-series.test.js`.
- `ea07008` — Ticket 4: the live exit law is no-target (thesis-invalidation exits, timers as backstops, protective stop kept); the D4 experiment flipped (REF = no-target, D4 = with-target counterfactual); `D4_TRAIL_CONTINUATION_*` → `D4_PLANNED_TARGET_*`. Frozen digests re-pinned (judge/*.js, watch/watch.js, all(48); execution/*.js unchanged); audit §4.8. `test/judge-watch.test.js` W06, `test/judge-experiment-replay.test.js` S01.
- `d4e9cc0` — Ticket P step 1 (non-frozen): the paper-fill latency signal. `gateway/collector.js` measures the per-source round-trip and writes `latency.kraken = { rttMs, door, observedAt }` into `matrix.json` (rttMs null + DEGRADED when the fetch failed); `readGatewayLatency` reads it back. `lib/paper-fill-latency.js` `effectiveFillLatencyMs` is the pure bounded delay model (measured base, reference fallback, degraded/unknown widening). `test/paper-fill-latency.test.js` PFL-1..6.
- `a32b9d4` — Ticket P step 2 (FROZEN): the paper fill delay is the live-measured Kraken round-trip, widened when degraded, not a fixed 250ms. `execution/paper-adapter.js` gains an optional `latencySource` (per-fill, bad reading → fixed fallback, behaviour-identical when absent); `composeJudge` gains `paperLatencySource` (null under REPLAY); fly.js supplies it from the gateway. Frozen digests re-pinned (judge `35f7be96…`, execution `676e1533…`, all `126b58d0…`; watch UNCHANGED); audit §4.9. `test/judge-adapters.test.js` P-LAT.
- `52f8a02` — Ticket Z part A: one open position at a time. `config/judge.paper.json` (and the byte-equal reference sample + the starting-styles config) `maxSimultaneousAssetPositions` 3 → 1; enforced at both `judge/risk.js` admission and `execution/reducer.js` commit (no frozen code change). Sizing unchanged and stays learning-gated.
- `2ead2e8` — Ticket Z part C (FROZEN): sizing side by side — two REPLAY-only arms. `SIZING_DEPTH_CAPPED` (full ladder, largest bite the book absorbs cleanly) vs `SIZING_PURE_ALL_IN` (fraction 1 alone, dormant/learning-gated until SIZING qualifies). `judge/experiment-replay.js` threads `dynamicSizing` per FUNDED arm; live sizing path byte-unchanged. Frozen digests re-pinned (judge `babd2f69…`, all `5ef55b6e…`; execution + watch UNCHANGED); audit §4.10. `test/judge-experiment-replay.test.js` S09.
- `00773af` — Ticket 6 (differentiator) step 1: the senses→case trigger plumbing. `market-lab/case-trigger.js` — flag-source-agnostic: enqueues a Socrates case for each flagged DECLARED research subject, per-coin cooldown, fail-closed, authority NONE. `test/case-trigger.test.js` CT-1..4.
- `5ebaecb` — Ticket Z part B (FROZEN): depth-capped whole-nut is the LIVE paper sizing law. `judge/size-ladder.js` `allInEvidence` ('REQUIRED' default | 'RISK_BOUNDED'); under RISK_BOUNDED fraction-1 drops the learning max-size gate and is bounded by the risk law (3% per-bite loss cap) + absorption. fly.js wires the ladder for PAPER; config loss law → 0.03. Frozen digests re-pinned (judge `a38fa9ed…`, all `1a287ba4…`); audit §4.11. `test/judge-size-ladder.test.js` RISK_BOUNDED.
- `435ea0f` — Ticket 6 addition: the Judge's active candidates as a second case-trigger source (`market-lab/judge-candidate-flag-source.js` + `combineFlagSources`), alongside fresh dossiers.
- `d9216cd` — Ticket 7 (SIM-1): `learning/daily-move-simulator.js` — a pure price-geometry detector over the study's prefix-only decision frames; forward bite scored separately; its own zero-credit-safe simulation record. `test/daily-move-simulator.test.js`.
- `84bb22b` — LEAN PASS 2 (1/2): retired the SOCIAL-5B offline research pipeline (`research/`, `research/referee/`, `persistence/social-research-export.js`, `bin/social-research.js`) + its test suites to attic. −4587 lines. Fences excluded attic/. Targets #2 (fixture providers) / #3 (dup readers) found NONE.
- `6e010c1` — LEAN PASS 2 (2/2): retired four orphaned tools/ smoke scripts to attic. −335 lines. Running app 82047 lines.
- `e8f0a54` — Ticket 8 step 1: the closed set of three paper accounts (`config/accounts.paper.json` + `lib/accounts.js`) — David/Cerulean/Cody, $500 each, per-account data dir + future key ENV names (null now). Step 2 (cockpit dropdown/view) pending David's viewing-model answer. `test/paper-accounts.test.js`.
- `c18adb4` — Strategy 6 (IFR): `market-lab/isolated-flush-reversal.js` pure SHADOW_ONLY detector (Kraken-only flush, references held, recovery-ownership ≥ 0.80, absorption). Authority NONE. `test/isolated-flush-reversal.test.js`.
- `f5ad985` — Strategy 7 (SDR): `market-lab/spot-confirmed-deleveraging-reclaim.js` pure SHADOW_ONLY detector (broad selloff + verified OI contraction + KF liquidation selling + all-three-venue bid recovery + perp discount repairing; smallest bite). Authority NONE. `test/spot-confirmed-deleveraging-reclaim.test.js`. IFR/SDR live wiring blocked on absent senses (no Binance book provider, no Kraken-Futures liquidation stream).
- `a2cfac1` — Hunt-trail: `lib/hunt-trail.js` pure assembler (arrival → research → decision+case → outcome, coherence-checked, exact-vs-coin-level link honesty) + GET-only `/api/hunt-trail?coin=` in ui/server.js (serpent page untouched). `test/hunt-trail.test.js`, `test/ui-endpoints.test.js` HUNT-TRAIL.
- Ticket 4 (the two new senses + IFR/SDR SHADOW_ONLY wiring; supersedes the "blocked on absent senses" note above): **Binance public book** provider (`market-lab/providers/binance.js`, provider `BINANCE_SPOT`, D16) — read-only combined WS (self-contained depth20 + trades), USDT-quoted with the honest native quote, a **frozen USD basis** held at exactly 1 and a **stablecoin-health gate** that fails the USD reference closed (a de-peg never reads as a flush); geo-blocked from this environment (`CODE_PATH_COMPLETE_EXTERNAL_ACCESS_BLOCKED` / `BLOCKED_GEOGRAPHY`, never evaded). **Kraken Futures public stream** (`market-lab/providers/kraken-futures-stream.js` on `KRAKEN_DERIVATIVES`, D03-B) — seq-numbered book, matched trades TAGGED with the venue's own `liquidation` type (a forced SELL == a LONG liquidated), and **native-unit OI** (`CONTRACTS` + the verified contractSize multiplier; a liquidation's quote notional is computed only from the verified contractSize, else PARTIAL/UNIT_UNVERIFIED). Both offline-tested with scripted WS, fail-closed, no credentials. **Wiring** (SHADOW_ONLY, authority NONE, fenced from Judge/Watch/execution): `market-lab/cross-venue-episode.js` assembles the IFR/SDR episodes from the senses (the Binance health gate + unit-verified OI contraction flow through to fail-closed refusals), `market-lab/cross-venue-shadow.js` runs the pure detectors and records episode + verdict + counterfactual. Live composition into the runtime loop still waits behind the paper publish. Registry: `BINANCE_SPOT` + endpoints; `PROVIDER_IDS`/owner FACTORIES/paper policy+profile/sensor inventory extended. `test/binance-provider.test.js`, `test/kraken-futures-stream.test.js`, `test/cross-venue-shadow.test.js`.
- Ticket 5 (PERSIST-1 step 4 — the real GCS object-store client): `persistence/gcs-client.js` — a dependency-free Google Cloud Storage JSON-API client (the Replit App Storage backend is GCS-backed) that mints + caches a service-account JWT (RS256 via node:crypto) over an INJECTED fetch, and does put/get/head/list/delete; the private key is used to sign and is NEVER logged, returned, or embedded in an error. `persistence/object-store.js` now COMMISSIONS the GCS provider: with `SERPENT_OBJECT_STORE_BUCKET` + `SERPENT_OBJECT_STORE_GCS_SERVICE_ACCOUNT_JSON` present it opens an ACTIVE GCS adapter (same contract as FILESYSTEM: prefix-rooted, key-validated, byte-capped, digest-reported); missing either is MISCONFIGURED (fail-closed); an unset provider stays DISABLED. `openObjectStore` builds the real client from the credential VALUE in production (the one place it is read, never logged) or takes an injected client in tests. The existing uploader + restore-on-boot run unchanged over the GCS adapter. **Secret NAMES David adds in Replit:** `SERPENT_OBJECT_STORE_PROVIDER=GCS`, `SERPENT_OBJECT_STORE_BUCKET=<bucket>`, `SERPENT_OBJECT_STORE_GCS_SERVICE_ACCOUNT_JSON=<service account JSON>` (the secret), optional `SERPENT_OBJECT_STORE_PREFIX`. Tests: end-to-end uploader→restore against the FILESYSTEM backend (existing) AND a mocked in-memory GCS client. `test/gcs-client.test.js`, `test/object-store-gcs.test.js`, updated `test/object-store.test.js` OS-1.
- Ticket 6 (CPU lanes — measure the main lane, keep heavy work off it): `lib/main-lane-meter.js` — a pure per-step timing meter (p50/p95/max + an over-budget count per label, bounded ring buffers) — wired into `learning/service.js`'s tick so each maintenance step (capture / maturation / learning / shadow) reports how long it occupies the main event loop that carries the tape and Judge, surfaced in the service status as `mainLane`. `lib/cpu-lane.js` + `lib/cpu-lane-worker.js` — a bounded worker-thread CPU lane that runs a REGISTERED PURE job off the loop with bounded V8 resources, a bounded response and a hard timeout; FAIL-CLOSED and non-throwing (disabled / worker error / timeout / early exit / oversized response all resolve to `{ok:false,code}` so the caller runs inline), with the daily-move **simulator** registered as its first job (the worker rebuilds the detector from serializable params). The two heaviest steps David named already run OFF the main loop — the **daily study + simulator** in one child process per job (`tools/daily-study-process-child.js`), the opportunity audit through a worker port, **archive finalize** at capture stop — so this ticket adds the measurement of the one in-loop maintenance path plus the reusable worker-lane substrate; the tape and Judge (FROZEN_FOR_PAPER) are untouched. `test/cpu-lane.test.js` (meter + lane, worker result byte-identical to inline). Fence: learning reuse-set + `lib/main-lane-meter.js`.
- Ticket 7 (paper-day readiness checklist): `docs/serpent/READINESS.md` — David-facing. The secret NAMES the publish needs (the five Required for `READY_FOR_PAPER`; the object-store four for durability, Ticket 5; the TALK-TO-THEM three — `ANTHROPIC_API_KEY`, `SERPENT_TALK_DAILY_USD` default 2, `SERPENT_SOCRATES_DAILY_USD` default 5); the gates that must be green (preflight A–L, the two CORE blocker groups → `READY_FOR_PAPER`, with the optional/paid/dark blockers reported-not-blocking); and the exact click order (set names → preflight → init-paper once → Run → watch the drawers → clean stop). NAMES only, never a value. Derived from paper/preflight.js, paper/readiness.js, .env.paper.example, persistence/object-store.js.
- TALK-TO-THEM (the LLM explainer wired end to end + the two protected durable toggles): the question box on the serpent page (`#ask` drawer), the read-only deterministic evidence answer (`paper/companion.js`) and the paid free-form LLM path (`paper/companion-chat.js`, Anthropic, `ANTHROPIC_API_KEY`, the model sees only the engine's evidence + the question, no tools, never feeds the Judge) were already built. This ticket adds the AMENDMENT: **two protected durable toggles** ASK / SOCRATES with today's-spend meters — `lib/explainer-toggles.js`, a store DECOUPLED from the safety control latches (its own atomic `<data>/control/explainer-toggles.json`, added as a backed object-store source so it survives a republish; safe defaults ASK on / SOCRATES off when the file is absent). The caps are the named `SERPENT_TALK_DAILY_USD` / `SERPENT_SOCRATES_DAILY_USD`, FAIL-CLOSED (unset / 0 = that half dormant; 2 / 5 are the documented recommended ceilings); the dispatcher's daily cap now reads `SERPENT_TALK_DAILY_USD` (legacy name kept as a fallback). `ui/server.js`: `/api/ask/status` carries the toggles + meters (`explainerControlsView`); a protected `POST /api/ask/toggle` flips a half (auth like a control latch, but changes no trading permission); the ASK toggle + the talk meter gate the chat dispatch (dormant → the recorded-evidence answer stands, nothing dispatched). `ui/index.html`: the two toggles + meters render in the Ask drawer and flip via the authenticated endpoint. Flipping needs no publish or secret edit. `test/explainer-toggles.test.js`, `test/ui-ask.test.js` ASK-HTTP-4.
- Ticket B (I/O lane — the bite lane never blocks on disk): `lib/write-behind.js` — a bounded, fail-closed write-behind buffer with two shapes (APPEND for the JSONL streams, LATEST-coalesced for the current book / status / feature files). The bite lane ENQUEUES (memory only) and returns; a background flusher (and the shutdown seam) writes. A flush error is caught + counted (never thrown into the bite lane); the append queue is bounded and an overflow drops the oldest with a counted loss (research capture, never a decision input). `tape/store.js` routes every trade / snapshot / event append and the current-book / status / feature writes through it OPT-IN (`SERPENT_TAPE_WRITE_BEHIND=true`; default OFF = today's synchronous freshest write); `flushTapeWrites` / `stopTapeWrites` drain it, wired into the tape's clean-stop so nothing is lost. Measure before/after on the Replit mount by toggling the env and reading `tapeWriteBehindStatus()` (flush ms, queued, dropped). `test/write-behind.test.js`, `test/tape-store-write-behind.test.js`. App 83,334 JS lines; largest source .js `rumor2/social-settle.js` 117 KB (a LEAN PASS 3 target).
- Ticket C (control-plane hardening — a second factor on every acting control): the cockpit stays **read-only without login**; acting authority is now granted only with password **+ a second factor** (RFC 6238 TOTP; secret by NAME `SERPENT_CONTROL_TOTP_SECRET`). `lib/totp.js` — a dependency-free (node:crypto only) TOTP verifier: base32 decode, HOTP dynamic truncation, ±1-step skew, constant-time compare, `totpSecretUsable`; it only ever answers a boolean and never echoes the secret or the code, and every malformed input fails **closed** to false. `ui/auth.js` `ControlAuth` gains the second factor: `login(password, totp)` requires a valid current code whenever a usable secret is provisioned, the granted session then the sole key to every acting control (KILL, CAGE, CLEAR, ASK / SOCRATES toggles); **CLEAR** and **ARM_LIVE** re-verify BOTH factors fresh at the instant they act; the code rides the **same** failed-auth limiter as the password (both factors are evaluated before the verdict so a password-correct / code-wrong attempt still counts a failure — no TOTP brute-force oracle). Fully backward compatible: with no secret provisioned the checks are exactly the former password-only ones (existing tests unchanged); a present-but-undecodable secret is treated as `DISABLED` (password-only, never a bricked cockpit), which is why `status()` surfaces `secondFactor: 'REQUIRED' | 'DISABLED'` and the paper-day checklist confirms `REQUIRED`. `ui/server.js` passes `body.totp` through login / ARM and strips it (with password / confirmPhrase) before the control layer; `ui/index.html` shows the 6-digit field on the login and CLEAR cards only when the second factor is required. Documented in `docs/serpent/READINESS.md` (a new control-plane-hardening subsection + the `SERPENT_CONTROL_TOTP_SECRET` NAME). No frozen surface touched (ui/*, lib/* are outside FROZEN_FOR_PAPER). `test/totp.test.js` (RFC 6238 Appendix B vectors), `test/control-auth-second-factor.test.js` (SF-1..7). App 83,514 JS lines; largest source .js `rumor2/social-settle.js` 117 KB (the first LEAN PASS 3 target).
- LEAN PASS 3 (split 1): `rumor2/social-settle.js` (117 KB) → a thin barrel over sibling concern modules `rumor2/social-settle-{common,events,reconciliation,operational,replay}.js` (each < 40 KB). Behavior byte-identical: the code is moved verbatim and the barrel re-exports the EXACT same 115 public names, so all ~10 importers and ~50 social/x test files are untouched. Siblings (not a subdirectory) so their `./`-relative imports satisfy the rumor-tier import allowlist (R2A-75); each sibling imports only what it uses, so only `-operational` carries the catalog/scope contracts and only `-common`/`-replay` carry the research types (the R2A-SOCIAL-8/9 authority allowlists were tightened to exactly those, never widened). First pushed at `49511ab` as a subdirectory; corrected here to siblings after the truth.js split surfaced that the subdir sub-files' `../` imports broke R2A-75 (a `git ls-files` fence the untracked-at-test-time files had hidden — the lesson: stage before verifying ls-files fences).
- LEAN PASS 3 (split 2): `rumor2/truth.js` (80 KB) → a thin barrel over sibling modules `rumor2/truth-{core,classify,graph,validate,txn,replay}.js` (each < 40 KB). Behavior byte-identical (code moved verbatim; only 10 private consts gained `export ` for reuse); the barrel re-exports the EXACT same 60 public names, so all 68 importers are untouched. Imports stay inside the R2A-75 allowlist (`node:*`, `./registry.js`, `./packet.js`, `../evidence/contract.js`, `./truth-*.js`). `rumor2/truth.js` is a SCOPE-8 protected surface, so its digest is re-pinned in `test/social-4f-scope.test.js` REPINNED (audited, byte-identical behavior; not a frozen-for-paper file). App 83,650 JS lines; largest source .js now `learning/adaptive-qualified-procedure.js` 84 KB (next split targets: it, then `rumor2/collector.js` 80 KB and `rumor2/x-runtime.js` 75 KB).
- LEAN PASS 3 (split 5): `market-lab/broad-day-reader.js` (65 KB) → a thin barrel over `market-lab/broad-day-reader-core.js` (29 KB: constants + pure helpers + `BroadDayReaderError`) and `market-lab/broad-day-reader-open.js` (40 KB: the `openBroadDayReader` function). Behavior byte-identical (verbatim line-slices; only `export ` prefixes added to private decls in `-core`); the barrel re-exports the EXACT same 8 public names. Imports stay market-lab-local + node. Not digest-pinned. Full suite 3001 pass / 0 fail; offline gate 0. App 83,974 JS lines; only remaining >60 KB source .js is `rumor2/collector.js` 80 KB (single closure, left) and `rumor2/x-runtime.js` 73 KB (the last splittable >60 KB target).
- CONTROL-0B (TOTP replay guard, follow-up to Ticket C): `lib/totp.js` gains `verifyTotpDetailed(secret, code, {…, afterCounter})` → `{ ok, counter }` reporting which RFC-6238 time step matched and refusing any match at a step `<= afterCounter` (the earliest acceptable step within ±window is returned so an attacker can't inflate the floor). `ui/auth.js` `ControlAuth` remembers the last accepted step in a durable, injectable replay store and passes it as the floor, advancing it ONLY on a fully-successful credential check — so a captured code cannot be replayed inside its own window, and a wrong-password attempt carrying a valid code never burns that step from under the real login. `ui/server.js` backs the store with an atomic file `<dataDir>/state/control_totp_replay.json` (read/write failure degrades to the in-memory floor, never blocks auth). Tests: `test/totp.test.js` TOTP-7, `test/control-auth-second-factor.test.js` SF-8 (single-use, failed-auth-doesn't-spend, durable floor survives a restart) + SF-5/6/7 advance the clock a step between accepted ops. Full suite 3001 pass / 0 fail; offline gate 0. App 83,888 JS lines; largest source .js `rumor2/collector.js` 80 KB.
- LEAN PASS 3 (split 4): `market-lab/contracts.js` (73 KB, 134 exports) → a thin barrel (`export *`) over sibling modules `market-lab/contracts-{core,registry,subject,payload,observation,coverage}.js` (each < 35 KB). Behavior byte-identical: PURE verbatim line-slices — not one line edited, not even an `export ` prefix needed (every private helper stayed within its own sibling). The barrel re-exports the EXACT same 134 public names. Siblings import only `node:crypto` + `./contracts-*.js` (an acyclic DAG rooted at `-core`), so no market-lab fence trips. Not digest-pinned. Full suite 2999 pass / 0 fail; offline gate 0. App 83,839 JS lines; largest source .js `rumor2/collector.js` 80 KB (a single `startRumor2` closure — not barrel-splittable byte-identically, left as-is). Remaining >60 KB splittable: `rumor2/x-runtime.js` 73 KB, `market-lab/broad-day-reader.js` 65 KB.
- LEAN PASS 3 (split 3): `learning/adaptive-qualified-procedure.js` (84 KB) → a thin barrel over sibling modules `learning/adaptive-qualified-procedure-{constants,core,consumer,publication,trial-decision,execution,validator,qualification-decision}.js` (each < 27 KB). Behavior byte-identical (code moved verbatim; only shared private consts/functions in `-core` gained `export ` for reuse); the barrel re-exports the EXACT same 35 public names, so all importers are untouched. Every sibling's cross-package imports stay inside the learning-fence allowlist (`../lib/{jsonl,config,book-walk,main-lane-meter}.js`) — in fact all redistributed imports are same-dir `./`. No digest pin (learning-fences is a readdir capability scan, not a byte pin). Full suite 2999 pass / 0 fail; offline gate 0. App 83,827 JS lines; largest source .js now `rumor2/collector.js` 80 KB (next targets: it, `rumor2/x-runtime.js` 75 KB, `market-lab/contracts.js` 75 KB).
- LEAN PASS 3 (attic the legacy doctrine, narrow scope per David): `git mv` the named heavyweights to `attic/` — `doctrine/SOCIAL.md` (216 KB), `docs/JUDGE-CLOSEOUT-ACCEPTANCE.md` (73 KB), `docs/MARKET-SOCRATES-ACCEPTANCE.md` (54 KB) — 343 KB of legacy doctrine out of the active tree; `docs/serpent/` is the going-forward doctrine and `docs/JUDGE-PAPER-AUDIT.md` stays. The two tests that READ a moved doc are repointed to the attic path (`test/social-4e-foundation.test.js` reads `attic/doctrine/SOCIAL.md`; `test/judge-focused-fences.test.js` C01 reads `attic/docs/JUDGE-CLOSEOUT-ACCEPTANCE.md` for its row provenance). The structured/data provenance in the two NON-pinned files is repointed (`market-lab/registry.js` `docs:` field + comment, `paper/inventory.js` `smokeEvidence` citations) and `docs/sensor-inventory.paper.json` regenerated to match (the derived-inventory fence). The inline `// see doctrine/SOCIAL.md §…` comment citations scattered through the rumor2 social modules are deliberately LEFT as-is: most sit inside SCOPE-8 / 4E-5 byte-pinned files, and repointing a comment there would churn exactly the byte-pinned fences that a docs move is not worth the risk of touching (per David); they are reference notes an LLM rarely follows, and nothing reads them as a file. The fuller doctrine sweep (MISSION.md, RESEARCH-GOLDEN.md, SOCRATES.md) is intentionally NOT done — those stay where C01 and other reads pin them. App 83,650 JS lines (unchanged — docs are not JS); largest source .js `learning/adaptive-qualified-procedure.js` 84 KB.
- `f510ec9` — Ticket 6 step 2: closed the senses→case seam. `market-lab/dossier-flag-source.js` flags a coin when its rumor2 dossierId is fresh; wired in fly.js opt-in (`SERPENT_CASE_TRIGGER=true`, default OFF), dormant until David funds the model. The "Talk to them" explainer was already built (paper/companion*.js). `test/dossier-flag-source.test.js` DFS-1..3. Fences: market-lab B08 allowlist.
- `d4e3f87` — Ticket 7 (DATA-1 sink): `market-lab/broad-day-archive-sink.js` opens the day archive, admits the accepted catalog, feeds the broad-Kraken collector's records via its `onRecord` seam, and finalizes + re-opens the session at the ET civil-day boundary so each completed day is study-eligible. Opt-in (`SERPENT_BROAD_DAY_ARCHIVE=true`, default OFF), fail-closed (a fault latches the sink dark, never breaks capture). Gated in `lib/collectors.js` (both DATA_ONLY and PAPER broad blocks). The >8% study law untouched. Fences updated (market-lab B08 allowlist, data-only source-shape). `test/broad-day-archive-sink.test.js` DAS-1..3. (SIM-1 simulator still design-only.)

## Test recipe
```
PERSIST_TEST_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:55432/cobra_test \
PERSIST_TEST_DATABASE_ISOLATED=THROWAWAY_SCHEMA_ONLY \
NODE_OPTIONS=--import=$PWD/test/helpers/offline-guard.mjs \
COBRA_OFFLINE_GUARD_LOG=/tmp/guard.jsonl COBRA_OFFLINE_GUARD_RUN=suite \
npm test -- --test-concurrency=1 ; node tools/offline-gate.mjs /tmp/guard.jsonl
```
